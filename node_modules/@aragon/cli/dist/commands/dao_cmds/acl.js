"use strict";

var viewCommand = require('./acl_cmds/view');

var _require = require('../../middleware'),
    manifestMiddleware = _require.manifestMiddleware,
    moduleMiddleware = _require.moduleMiddleware,
    environmentMiddleware = _require.environmentMiddleware;

var MIDDLEWARES = [manifestMiddleware, moduleMiddleware, environmentMiddleware];
exports.command = 'acl <dao>';
exports.describe = 'Shortcut for aragon dao acl view <dao>';

exports.builder = function (yargs) {
  return yargs.commandDir('acl_cmds', {
    visit: function visit(cmd) {
      cmd.middlewares = MIDDLEWARES;
      return cmd;
    }
  });
};

exports.handler = viewCommand.handler;